<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="3.4-migrate-existing-users-to-auth-provider" author="naborshchikov">
        <sql>
            INSERT INTO auth_provider (id, user_id, provider_type, provider_id)
            SELECT gen_random_uuid(), id, 'TELEGRAM', tg_id
            FROM users
            WHERE tg_id IS NOT NULL;
        </sql>
    </changeSet>

    <changeSet id="3.4-assign-user-role-to-existing-users" author="naborshchikov">
        <sql>
            INSERT INTO user_role (id, user_id, role)
            SELECT gen_random_uuid(), id, 'USER'
            FROM users;
        </sql>
    </changeSet>
</databaseChangeLog>
